name: flutter_simple_typewriter
description: "A Flutter package that adds a typewriter effect to text fields, enhancing user interface interactivity."
version: 1.0.2
homepage: https://github.com/musfique113/flutter_simple_typewriter

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

topics:
  - animation
  - typewriter


platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

flutter:
